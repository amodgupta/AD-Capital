import groovy.sql.Sql

allprojects {
	apply plugin : 'idea'
	apply plugin : 'groovy'
	repositories {
		mavenCentral()
	}
	sourceCompatibility = 7;

}

compileJava {
	options.compilerArgs = ['-XDignore.symbol.file=true'];
}